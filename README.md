Proyecto página de ventas de libros (HTML - Css - JS)
una págína principal - aplica o no cookies (se guardan el localStorage) // aplica geolocalización  - (index - contiene dentro estilo css - y JS) 
pagina de acceso a la compra del producto (index1.html /index.css /index1.js) -  Libros con precio (al tocar la imagen se abre la reseña en un alert) al aceptar la compra de cada ejemplar ,se guarda en un carrito
(localStorage) aplica fetch  - (se utiliza Xampp) - La informaciòn de cada libro se guarda en un Json y con otro Fetch se utilzia un txt con un frase. - 
La página de contacto para enviar datos.  (index2.html /css)- 
